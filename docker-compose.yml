services:
  app:
    build: .
    image: viktor-cistic:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      DATABASE_URL: file:/data/prod.db
    env_file:
      - .env
    volumes:
      - viktor-data:/data

volumes:
  viktor-data:
